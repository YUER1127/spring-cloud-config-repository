server:
  port: 6331
  
spring:
  cloud:
    config:
      retry:
        multiplier: 1.1
        initial-interval: 1999
        max-interval: 2000
        max-attempts: 6
         
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
      
management:
  endpoints:
    web:
      exposure:
        include: '*'
